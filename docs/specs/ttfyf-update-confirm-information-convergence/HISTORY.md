# History: 更新确认弹窗信息收敛（#ttfyf）

## 2026-05-05

- 创建规格，冻结单服务确认与聚合预览的业务组件边界。
- 决策：单服务确认显示完整版本证据与策略字段；聚合确认保持轻量列表摘要，只共享底层版本展示语义，不复用单服务业务外壳。
- 完成实现：新增单服务确认详情组件，导航页、服务详情页与 Operations/Services 服务级确认统一接入；聚合预览继续保持独立列表边界。
- 完成 Storybook 与视觉证据：覆盖导航页单服务确认、服务详情单服务确认、floating tag、同标签新 digest 与长 digest 窄屏场景。
- 细化布局：弹窗内镜像与版本信息改为扁平堆叠，去掉无意义缩进，保持一行版本主结论的扫读效率。
- 再次收敛：目标 digest 只显示一次，但完整值必须直接可见，避免省略削弱人工校验。
- 再次收敛：移除单服务确认正文中的 `范围 service`、标题已表达的服务名与默认 `备份 inherit`，保留请求契约不变。
- 视觉强化：将单服务确认正文中的 `updatable` 与 `disallow` 改为语义 badge，提升状态与策略值的辨识度。
- 生产化加固：状态 badge 改为按语义映射色调，并补充长状态文本窄宽不溢出的 Storybook 覆盖。
- 审计收口：补充 badge computed contrast 断言、hint/长状态/浅色主题视觉证据，关闭剩余 P3 风险。
