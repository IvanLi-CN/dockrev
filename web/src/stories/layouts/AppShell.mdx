import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as AppShellStories from './AppShell.stories'

<Meta of={AppShellStories} />

# AppShell Navigation

Desktop navigation supports the normal expanded shell, the compact icon-only shell, and a detail layout with a dedicated service tree rail. On mobile, primary navigation moves to the bottom bar while the drawer is reserved for page-level secondary content such as the service tree.

<Canvas of={AppShellStories.Overview} />

<Canvas of={AppShellStories.CollapsedSidebar} />

<Canvas of={AppShellStories.DetailSidebarDesktop} />

<Canvas of={AppShellStories.MobileBottomNavAndDrawer} />
