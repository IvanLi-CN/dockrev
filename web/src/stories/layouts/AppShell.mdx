import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as AppShellStories from './AppShell.stories'

<Meta of={AppShellStories} />

# AppShell Navigation

The AppShell uses one desktop sidebar with the Logo, five primary page icons, a page-owned context directory, and a fixed footer. On mobile the primary navigation moves to the bottom bar and the same context directory opens in the top drawer.

<Canvas of={AppShellStories.Overview} />

<Canvas of={AppShellStories.SingleSidebarContext} />

<Canvas of={AppShellStories.DetailSidebarDesktop} />

<Canvas of={AppShellStories.OverviewWithSidebarIdentityPopover} />

<Canvas of={AppShellStories.MobileBottomNavAndDrawer} />
