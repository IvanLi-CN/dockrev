import { Meta, Primary, Stories, Title, Description } from '@storybook/addon-docs/blocks'
import * as ServiceResourcePanelStories from './ServiceResourcePanel.stories'

<Meta of={ServiceResourcePanelStories} />
<Title />
<Description>
`ServiceResourcePanel` 现在把实时状态提升到头部 badge，并把指标切换与时间窗口整理成同一工具栏，同时保留 SSE 实时状态与历史趋势图逻辑；离线时则退化为本地缓存只读态。
</Description>

## 关注点
- 顶部 Hero 会展示实时状态 badge、窗口/样本/最近更新时间 facts。
- `Tabs` 负责 CPU / 内存 / 网络 / 磁盘 I/O / PIDs 的指标切换，`ToggleGroup` 负责 15m / 1h / 6h 时间窗口。
- Story 使用 mock API，覆盖默认、空数据、监控关闭、流错误，以及离线缓存只读态。

<Primary />
<Stories />
