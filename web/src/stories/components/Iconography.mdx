import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import * as Stories from './Iconography.stories'

<Meta of={Stories} />

# Iconography

共享 icon helpers 统一收敛在这里，避免为每个 SVG helper 单独维护一页 story。业务图标仍然允许继续使用 Iconify；这里只覆盖 app 内的少量基础箭头/刷新/删除，以及 repo / registry 的常用平台图标。

## 示例

<Canvas of={Stories.Catalog} />
